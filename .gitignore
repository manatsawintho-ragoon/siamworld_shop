# ── Dependencies ─────────────────────────────────────────────
node_modules/

# ── Build outputs ─────────────────────────────────────────────
dist/
.next/
out/
build/

# ── Environment ── (NEVER commit .env — contains secrets) ────
.env
.env.local
.env.*.local
# Deploy/panel env files (real secrets) — *.example files stay committable
panel.env
deploy/panel.env
deploy/customers/*/.env

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*

# ── OS / Editor ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Docker overrides (local-only) ─────────────────────────────
docker-compose.override.yml

# ── Database / Misc ───────────────────────────────────────────
*.sqlite
*.sqlite3
*.bak
coverage/
*.tsbuildinfo

# ── Internal tools (not needed by the team) ───────────────────
CLAUDE.md
.agents/
skills-lock.json
.claude/
.openclaude-profile.json

# ── Local scratch files (screenshots, throwaway scripts) ─────
fix_theme.js
easy_slip_plan.png

# ── Operational / sensitive infra files ───────────────────────
network_investigation_report.txt
deploy/customers.json
deploy/certs/

# nlogin plugin runtime data — live DB credentials, user DB (password hashes),
# player logs. Never commit.
plugins_nlogin/
