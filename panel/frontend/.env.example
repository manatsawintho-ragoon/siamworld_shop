# Panel frontend environment variables
# Copy to .env.local and fill in values before running

# URL of the panel backend API (as seen from the browser)
# Example: https://panel.siamsite.shop (production) or http://localhost:5000 (dev)
NEXT_PUBLIC_API_URL=http://localhost:5000
