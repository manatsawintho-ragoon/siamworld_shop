import PageSkeleton from '@/components/PageSkeleton';

/** Instant feedback on navigation - see components/PageSkeleton.tsx. */
export default function Loading() {
  return <PageSkeleton rows={10} />;
}
